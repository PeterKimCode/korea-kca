# GTCC 홈페이지 관리자 사용법

아버님용 간단 사용 설명서입니다. GitHub와 코드는 사용하지 않습니다.

## 시작하기

1. 인터넷에서 `홈페이지주소/admin/`을 엽니다.
2. 본인 이메일과 비밀번호를 입력합니다.
3. 화면 왼쪽에서 `강좌 관리`, `공지 관리`, `슬라이드 관리` 중 하나를 누릅니다.

로그인은 같은 컴퓨터에서 유지됩니다. 공용 컴퓨터에서는 작업 후 반드시 `로그아웃`을 누릅니다.

## 강좌 올리기

1. `강좌 관리`를 누릅니다.
2. `새 강좌 추가`를 누릅니다.
3. 강좌명, 분야, 표시 위치, 해시태그, 소개와 교육 내용을 입력합니다.
4. `사진 선택`을 눌러 사진을 고릅니다.
5. `미리보기`로 확인합니다.
6. `저장하고 바로 공개`를 누릅니다.

사진은 자동으로 홈페이지에 알맞게 줄어듭니다. 새 강좌를 저장하면 메인 카드, 전체강좌, 해시태그 필터, 강좌 소개 페이지가 함께 만들어집니다.
입력한 모든 해시태그가 전체강좌 상단 필터로 표시되며, 같은 태그를 반복해서 입력해도 하나만 저장됩니다.

## 공지 올리기

1. `공지 관리`를 누릅니다.
2. `새 공지 추가`를 누릅니다.
3. 제목, 내용, 작성자와 게시일을 입력합니다.
4. `미리보기`로 확인한 뒤 `저장하고 바로 공개`를 누릅니다.

새 공지는 공지사항 목록과 메인 페이지 최신 공지에 동시에 표시됩니다.

## 슬라이드 올리기

1. `슬라이드 관리`를 누릅니다.
2. `새 슬라이드 추가`를 누릅니다.
3. 사진을 고르고 사진 설명을 입력합니다.
4. 저장한 뒤 목록의 `위로`, `아래로` 버튼으로 순서를 바꿉니다.

마우스로 끌지 않아도 순서를 바꿀 수 있습니다.

## 수정, 숨기기, 복구하기

- 글이나 사진을 고치려면 해당 항목의 `수정`을 누릅니다.
- 잠시 보이지 않게 하려면 수정 화면에서 `홈페이지에 공개`를 끕니다.
- 삭제하려면 `삭제`를 누릅니다. 바로 없어지지 않고 휴지통으로 이동합니다.
- 잘못 삭제했다면 `휴지통` → `복구하기`를 누릅니다.
- 삭제한 내용은 30일 동안 복구할 수 있습니다.
- 예전 내용으로 되돌리려면 `변경 기록`에서 원하는 시점의 `이 시점으로 복구`를 누릅니다.

## 기본 강좌가 보이지 않을 때

관리자 화면 위쪽의 `기본 내용 가져오기`를 누르면 현재 등록되지 않은 기본 강좌, 공지, 슬라이드만 추가됩니다. 직접 만든 강좌와 기존 수정 내용은 삭제되거나 덮어쓰이지 않습니다.

## 꼭 기억할 것

- `저장하고 바로 공개`를 누르면 홈페이지에 즉시 반영됩니다.
- 저장하기 전 `미리보기`에서 제목과 사진을 한 번 확인합니다.
- 다른 사람이 먼저 같은 내용을 수정했다는 안내가 나오면 목록을 새로 불러온 뒤 다시 수정합니다.
- 화면 오른쪽 위의 물음표 버튼을 누르면 사용 순서를 언제든 다시 볼 수 있습니다.
